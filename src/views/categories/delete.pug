extends ../layout

block content
  h1.text-2xl.mb-8.font-bold= title
  if items.length
    p Delete the following items before deleting the category:
    ul.ml-2.mt-2
      each item in items
        li.mb-2.list-disc
          a.underline(href=item.url)= item.name
  else
    p Are you sure you want to delete this category?
    form.mt-2.flex.gap-4(method="POST")
      input#id.form-control(type='hidden', name='id', value=category._id )
      button.rounded-lg.border.border-grey-500.py-1.px-2.mb-6(type="submit" class="hover:bg-gray-300") Delete
      a.rounded-lg.border.border-grey-500.py-1.px-2.mb-6(href="/categories" class="hover:bg-gray-300") Cancel
